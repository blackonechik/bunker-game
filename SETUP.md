# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Настройка MySQL

Создайте базу данных:

```sql
CREATE DATABASE bunker_game CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

## Шаг 3: Настройка .env.local

Скопируйте `.env.example` в `.env.local` и заполните значения:

```bash
cp .env.example .env.local
```

Отредактируйте `.env.local`:

```env
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=your_password  # ← Ваш пароль MySQL
DB_DATABASE=bunker_game
JWT_SECRET=random_secret_key_here  # ← Сгенерируйте случайную строку
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

## Шаг 4: Запуск приложения

```bash
npm run dev
```

Приложение запустится на `http://localhost:3000`

TypeORM автоматически создаст таблицы при первом запуске.

## Шаг 5: Заполнение базы данных

После запуска приложения, выполните seed:

```bash
# Вариант 1: через API
curl http://localhost:3000/api/seed

# Вариант 2: через CLI скрипт
npm run seed
```

## ✅ Готово!

Теперь вы можете:
1. Открыть `http://localhost:3000`
2. Создать комнату
3. Пригласить друзей по коду или ссылке
4. Начать игру (минимум 4 игрока)

## 🆘 Проблемы?

### MySQL не подключается

```bash
# Проверьте, запущен ли MySQL
mysql -u root -p

# Проверьте порт
netstat -an | grep 3306
```

### TypeORM ошибки

```bash
# Пересоздайте БД
mysql -u root -p
DROP DATABASE bunker_game;
CREATE DATABASE bunker_game CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Socket.io не работает

- Проверьте консоль браузера
- Убедитесь что `NEXT_PUBLIC_APP_URL` правильный
- Перезапустите dev сервер

---

Подробная документация в [README.md](./README.md)
